const config = {
    db: {
        // MySQL database configuration
        host: "localhost",
        user: "root",
        password: "",
        database: "afrobuild_db",
        connectTimeout: 60000
    },
    listPerPage: 10,
};
module.exports = config;